
name: Docker Build stage

on:
  push:
    branches:
      - stage

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Login to Azure Container Registry
      uses: azure/docker-login@v1
      with:
        login-server: ${{ secrets.ACR_LOGIN_SERVER_stage }}
        username: ${{ secrets.ACR_USERNAME_stage }}
        password: ${{ secrets.ACR_PASSWORD_stage }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v2
      with:
        context: .
        push: true
        tags: acr0ai4accountancy0stage.azurecr.io/backend-ai4accountancy-stage-northeurope:latest
